# MERIVUS 贡献指南

感谢你关注 MERIVUS。MERIVUS 是基于开源 QGroundControl 技术体系二次开发的多无人机调度地面站，贡献内容应同时尊重上游许可证、项目架构边界和飞行安全要求。

## 贡献范围

欢迎围绕以下方向贡献：

- MERIVUS 主界面、地图交互、状态栏和任务面板；
- 多机选择、编队、航点队列和调度交互；
- 飞控连接、MAVLink 状态、TCP 网络链路和视频链路展示；
- 环境、姿态、IMU、电机、ESC、告警和日志 UI；
- Mock Link、SITL、日志解析和开发工具；
- 文档、设计规范、构建说明和安全检查清单。

涉及飞控命令、任务队列、目标解析、参数写入、AI 意图转换或批量操作的修改，必须提供清晰的安全边界说明和验证方法。

## 架构原则

- QML 负责界面呈现、输入和轻量视图状态；
- C++ 控制器或服务负责目标解析、几何计算、命令队列、安全校验、持久化、通信和异步任务；
- 优先复用 QGC 控件、Palette、Vehicle、MultiVehicleManager、Mission API 和已有通信链路；
- 优先使用 `custom/` 扩展点，避免无必要地大范围修改上游 QGC 文件；
- 不在 QGC 进程内创建竞争性的 MAVLink 连接；
- 区分飞控状态、链路状态、命令状态和 UI 状态。

## 飞行安全要求

提交代码前请确认：

- 没有默认广播到所有无人机的隐式命令；
- 多机操作携带明确、冻结、可见的目标系统 ID；
- 起飞、降落、返航、解锁、任务替换、参数写入和紧急动作保留显式确认；
- 对离线、遥测陈旧、重复 ID、无效坐标、不支持命令和部分失败有清晰处理；
- 不将自由文本、AI 输出或未验证脚本直接发送到 MAVLink 或 Vehicle；
- 自动化命令测试仅使用 Mock Link、PX4 SITL 或 ArduPilot SITL。

## 分支、提交与评审

建议工作流：

```bash
git pull --rebase
git checkout -b feature/short-description
# 修改与验证
git status
git add <files>
git commit -m "简要说明本次修改"
git push
```

提交说明建议包含：

- 修改目标；
- 影响范围；
- 验证方式；
- 未验证内容或已知风险。

## 不应提交的内容

请不要提交：

- Qt Creator `.user` 文件；
- `build/`、`debug/`、`release/`、`.qmake.stash` 等构建产物；
- 日志、截图、缓存和临时目录；
- 真实无人机坐标、生产任务数据或飞行敏感信息；
- APN、SIM、云服务 Token、SSH key、keystore、证书、账号密码；
- 未经确认许可证兼容性的第三方代码。

## 文档与 UI 贡献

UI 修改应遵循 `design-system/MASTER.md`，并满足：

- 状态不能只依靠颜色表达；
- 地图和告警上下文不应被大面积遮挡；
- 多机目标数量、系统 ID 和命令范围应可见；
- 需要考虑 1920x1080、1366x768、深色/浅色主题和中英文长文本；
- 不引入不必要的第三方 UI 框架、字体或运行时依赖。

## 许可证

MERIVUS 基于 QGroundControl 开源代码二次开发。贡献者提交代码时，应确认其贡献为原创或来自许可证兼容的来源，并同意在本项目的开源许可证约束下分发。详情参见 [COPYING.md](COPYING.md)。
